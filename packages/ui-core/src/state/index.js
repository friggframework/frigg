/**
 * State management utilities
 * Framework-agnostic state managers for UI components
 */

export { 
  ToastManager, 
  toastManager, 
  toast, 
  reducer as toastReducer,
  actionTypes as toastActionTypes 
} from './ToastManager.js';