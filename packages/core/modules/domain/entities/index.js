/**
 * Domain Entities Index
 * Export all domain entities for convenient importing
 */

const { AuthorizationSession } = require('./AuthorizationSession');

module.exports = {
    AuthorizationSession,
};
