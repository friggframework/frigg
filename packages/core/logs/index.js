const {
    debug,
    initDebugLog,
    flushDebugLog,
    redactSensitive,
    SENSITIVE_KEYS,
} = require('./logger');

module.exports = {
    debug,
    initDebugLog,
    flushDebugLog,
    redactSensitive,
    SENSITIVE_KEYS,
};
