{
  "name": "friggframework.org",
  "version": "1.0.0",
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "node-fetch": "^2.6.7",
    "dotenv": "^16.0.1"
  },
  "scripts": {
    "functions": "NODE_OPTIONS=--inspect netlify functions:serve",
    "netlify dev": "NODE_OPTIONS=--inspect netlify dev"
  }
}
