const ParentManager = require('../../modules/HubSpot/Manager.js');

class HubSpotManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = HubSpotManager;
