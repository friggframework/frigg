const ParentManager = require('../../modules/Salesloft/Manager');

class SalesloftManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = SalesloftManager;
