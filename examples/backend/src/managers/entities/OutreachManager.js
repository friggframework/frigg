const ParentManager = require('../../modules/Outreach/Manager');

class OutreachManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = OutreachManager;
