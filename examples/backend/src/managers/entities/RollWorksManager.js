const ParentManager = require('../../modules/RollWorks/Manager.js');

class RollWorksManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = RollWorksManager;
