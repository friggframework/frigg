describe('Ironclad API Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});

describe('Ironclad Integration Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});