describe('Yotpo API Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});

describe('Yotpo Integration Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});
