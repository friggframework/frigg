const ParentManager = require('../../modules/Stack/Manager.js');

class StackManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = StackManager;
