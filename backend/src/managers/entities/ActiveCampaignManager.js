const ParentManager = require('../../modules/ActiveCampaign/Manager');

class ActiveCampaignManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = ActiveCampaignManager;
