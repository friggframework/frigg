const ParentManager = require('../../modules/ConnectWise/Manager.js');

class ConnectWiseManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = ConnectWiseManager;
