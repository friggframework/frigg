const ParentManager = require('../../modules/Rev.io/Manager.js');

class RevioManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = RevioManager;
