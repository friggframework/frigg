const ParentManager = require('../../modules/Marketo/Manager.js');

class MarketoManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = MarketoManager;
