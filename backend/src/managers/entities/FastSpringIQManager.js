const ParentManager = require('../../modules/FastSpringIQ/Manager.js');

class FastSpringIQManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = FastSpringIQManager;
