const ParentManager = require('../../modules/Attentive/Manager');

class AttentiveManager extends ParentManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = AttentiveManager;
